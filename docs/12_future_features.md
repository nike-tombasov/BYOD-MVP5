## 13. Future features and later stages

Current clarification:
- real backend channel multitrack recording is a **future feature**;
- current MVP baseline has only recording state markers/runtime placeholders;
- real file recording pipeline is planned only after MVP pilots.

Planned future topics:
- real backend channel multitrack recording implementation
- recording files lifecycle/rotation/download operator workflow
- noise gate
- audio processing
- RMS visualizer
- statistics
- room dashboard
- security hardening improvements
- adaptation of local Windows workflows to Ubuntu VPS operations
- field MVP load simulation
- one-click VPS deploy maturity improvements
- moving HTML to production domain
- Admin Web UI
- adaptiveStream (discussion)
- dynacast (discussion)
- audio packet pacing (discussion)
- pre-warm publishing (discussion)
- backend-driven language selection by ui_lang (no priority)
- dynamic status text changes during event runtime (no priority)
- server-side real-time language personalization for listener texts (no priority)
- WS ACK/NACK message flow (no priority)
- request_id deduplication cache model (no priority)
- reconnect session resume model (no priority)
- CDN fallback logic (no priority)
- SRI / CSP for listener SDK loading (no priority)
- SDK cache busting strategy (no priority)
- formal deploy artifact manifest (no priority)
- strict stress metrics thresholds (no priority)
- advanced retry policies (no priority)
